{
  "name": "@holo-host/web-sdk",
  "version": "0.1.0",
  "description": "Client-side SDK for Holo Hosting hApp developer",
  "main": "./dist/index.js",
  "scripts": {
    "lint": "tslint -p ./tslint.json",
    "unit_test": "npm run build:lib && mocha ./test/*.js -r chai/register-should",
    "test": "npm run lint && npm run unit_test",
    "prepublishOnly": "npm test && npm run lint",
    "prepare": "npm run build && npm run build:lib",
    "preversion": "npm run lint",
    "postversion": "git push && git push --tags"
  },
  "files": [
    "lib"
  ],
  "author": "",
  "license": "GPL-3",
  "devDependencies": {
    "@types/node": "^12.7.12",
    "braintree-jsdoc-template": "^3.3.0",
    "chokidar-cli": "^2.0.0",
    "jsdoc": "^3.6.3",
    "typescript": "^3.6.4",
    "webpack": "^4.41.0",
    "webpack-cli": "^3.3.9"
  },
  "dependencies": {}
}
